# Python bytecode
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Virtual environment
.venv/

# Tool caches
.pytest_cache/
.ruff_cache/

# Secrets. API keys go here, never in the repo
.env

# Windows
Thumbs.db
desktop.ini

# Cached model output, saved cases, generated art, scratch.
#
# All of it is reproducible or personal, and the art is megabytes. If a case is
# good enough to keep in the repo, copy its file out of var/cases into
# tests/fixtures and commit that deliberately.
var/
